import { Dimensions } from 'react-native';

export const { width: SCREEN_WIDTH } = Dimensions.get('window');
export const MIN_DISTANCE = 30;
export const CAROUSEL_BUFFER_SIZE = 2; // buffer to create the looping effect
